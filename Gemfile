source "https://rubygems.org"

# Local preview of the site. GitHub Pages builds the site remotely, so this
# Gemfile is only used for `bundle exec jekyll serve` during development.
gem "jekyll", "~> 4.3"
gem "webrick", "~> 1.8"
